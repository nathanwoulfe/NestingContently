/**
 * Alias of the condition that gates the toggle-visibility block action so its button only appears
 * on blocks whose element type declares `umbracoNaviHide`. Shared by the condition manifest and the
 * action manifest that references it.
 */
export const TOGGLE_VISIBILITY_CONDITION_ALIAS = 'NestingContently.Condition.BlockHasVisibilityToggle';
